@using EndangeredNearYou.Web.Models
@model LocationViewModel

<h2>Create a New Location</h2>

@using (Html.BeginForm("InsertLocationToDatabase", "Location", "Post"))
{
    <input asp-for="City_Id" type="hidden" value="" />

    <label asp-for="Country_Code" class="control-label">Name</label>
    <input type="text" name="Name" class="form-control" value="" />

    <label asp-for="State" class="control-label">Price</label>
    <input type="number" step="0.01" name="Price" class="form-control" />

@*     <div class="form-group">
        <label for="">Select a Category</label>
        <select class="form-control" name="CategoryID" value="">
            @foreach (var cat in Model.Categories)
            {
                <option value="@cat.CategoryId">@cat.CategoryId @cat.Name</option>
            }
        </select>
    </div> *@

    <input type="submit" value="Create Location" class="btn btn-primary" />
}